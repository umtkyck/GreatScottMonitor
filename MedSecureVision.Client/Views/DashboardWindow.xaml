<Window x:Class="MedSecureVision.Client.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MedSecure Vision - Dashboard" Height="600" Width="800"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <!-- Main Container -->
    <Border CornerRadius="20" ClipToBounds="True">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0D1117" Offset="0"/>
                <GradientStop Color="#161B22" Offset="0.5"/>
                <GradientStop Color="#0D1117" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.8" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,16,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#58A6FF" Offset="0"/>
                                <GradientStop Color="#A371F7" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="📊" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Dashboard" FontSize="20" FontWeight="SemiBold" 
                                   Foreground="#F0F6FC" FontFamily="Segoe UI Variable"/>
                        <TextBlock Text="Manage your face enrollment" FontSize="11" 
                                   Foreground="#8B949E" FontFamily="Segoe UI Variable"/>
                    </StackPanel>
                </StackPanel>

                <Button Grid.Column="1" Style="{StaticResource IconButton}" Click="CloseButton_Click">
                    <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                          Width="14" Height="14" Stretch="Uniform"/>
                </Button>
            </Grid>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="24,0,24,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="300"/>
                </Grid.ColumnDefinitions>

                <!-- Enrolled Faces List -->
                <Border Grid.Column="0" Style="{StaticResource GlassCard}" Margin="0,0,12,0" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                            <TextBlock Text="👤" FontSize="18" Margin="0,0,10,0"/>
                            <TextBlock Text="Enrolled Faces" FontSize="16" FontWeight="SemiBold" 
                                       Foreground="#F0F6FC" VerticalAlignment="Center"/>
                        </StackPanel>

                        <ListBox x:Name="FacesList" Grid.Row="1" Background="Transparent" 
                                 BorderThickness="0" Foreground="#F0F6FC">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#21262D" CornerRadius="10" Padding="16" Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Border Grid.Column="0" Width="50" Height="50" CornerRadius="25" 
                                                    Background="#30363D" Margin="0,0,12,0">
                                                <TextBlock Text="👤" FontSize="24" 
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" 
                                                           Foreground="#F0F6FC"/>
                                                <TextBlock Text="{Binding EnrolledDate}" FontSize="11" 
                                                           Foreground="#8B949E"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                                <Button Content="✏️" Width="36" Height="36" 
                                                        Style="{StaticResource IconButton}" 
                                                        Click="EditFace_Click" Tag="{Binding}"/>
                                                <Button Content="🗑️" Width="36" Height="36" 
                                                        Style="{StaticResource IconButton}"
                                                        Click="DeleteFace_Click" Tag="{Binding}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Empty State -->
                        <StackPanel x:Name="EmptyState" Grid.Row="1" VerticalAlignment="Center" 
                                    HorizontalAlignment="Center">
                            <TextBlock Text="👤" FontSize="48" HorizontalAlignment="Center" Opacity="0.3"/>
                            <TextBlock Text="No faces enrolled" FontSize="16" Foreground="#8B949E" 
                                       HorizontalAlignment="Center" Margin="0,12,0,0"/>
                            <Button Content="Enroll Now" Style="{StaticResource PrimaryButton}" 
                                    Margin="0,16,0,0" Click="EnrollNow_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Stats Panel -->
                <StackPanel Grid.Column="1">
                    <!-- Status Card -->
                    <Border Style="{StaticResource GlassCard}" Padding="20" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="Status" FontSize="14" Foreground="#8B949E" Margin="0,0,0,12"/>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse x:Name="StatusDot" Width="12" Height="12" Margin="0,0,10,0">
                                    <Ellipse.Fill>
                                        <SolidColorBrush Color="#3FB950"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                                <TextBlock x:Name="StatusText" Text="Active" FontSize="18" 
                                           FontWeight="SemiBold" Foreground="#F0F6FC"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Stats Card -->
                    <Border Style="{StaticResource GlassCard}" Padding="20" Margin="0,0,0,12">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                                <TextBlock Text="Total Authentications" FontSize="12" Foreground="#8B949E"/>
                                <TextBlock x:Name="TotalAuthsText" Text="0" FontSize="32" 
                                           FontWeight="Bold" Foreground="#58A6FF"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Margin="0,0,0,16">
                                <TextBlock Text="Success Rate" FontSize="12" Foreground="#8B949E"/>
                                <TextBlock x:Name="SuccessRateText" Text="--%" FontSize="24" 
                                           FontWeight="SemiBold" Foreground="#3FB950"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2">
                                <TextBlock Text="Last Authentication" FontSize="12" Foreground="#8B949E"/>
                                <TextBlock x:Name="LastAuthText" Text="Never" FontSize="14" Foreground="#F0F6FC"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Actions Card -->
                    <Border Style="{StaticResource GlassCard}" Padding="20">
                        <StackPanel>
                            <TextBlock Text="Quick Actions" FontSize="14" Foreground="#8B949E" Margin="0,0,0,12"/>
                            <Button Content="➕ Enroll New Face" Style="{StaticResource ModernButton}" 
                                    Click="EnrollNow_Click" Margin="0,0,0,8"/>
                            <Button Content="🔄 Re-enroll Face" Style="{StaticResource ModernButton}" 
                                    Click="ReenrollFace_Click" Margin="0,0,0,8"/>
                            <Button Content="📤 Export Data" Style="{StaticResource ModernButton}" 
                                    Click="ExportData_Click"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>

