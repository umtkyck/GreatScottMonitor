<Window x:Class="MedSecureVision.Client.Views.FallbackAuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Alternative Authentication" Height="500" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <!-- Main Container -->
    <Border CornerRadius="20" ClipToBounds="True">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0D1117" Offset="0"/>
                <GradientStop Color="#161B22" Offset="0.5"/>
                <GradientStop Color="#0D1117" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.8" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ═══════════════════════════════════════════════════════════════
                 HEADER
            ═══════════════════════════════════════════════════════════════ -->
            <Grid Grid.Row="0" Margin="24,20,24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <!-- Key Icon -->
                    <Border Width="36" Height="36" CornerRadius="10" Margin="0,0,12,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#D29922" Offset="0"/>
                                <GradientStop Color="#F0883E" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Path Data="M12.65,10C11.83,7.67 9.61,6 7,6A6,6 0 0,0 1,12A6,6 0 0,0 7,18C9.61,18 11.83,16.33 12.65,14H17V18H21V14H23V10H12.65M7,14A2,2 0 0,1 5,12A2,2 0 0,1 7,10A2,2 0 0,1 9,12A2,2 0 0,1 7,14Z"
                              Fill="White" Stretch="Uniform" Width="18" Height="18"
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Alternative Login" FontSize="18" FontWeight="SemiBold" 
                               Foreground="#F0F6FC" VerticalAlignment="Center"
                               FontFamily="Segoe UI Variable"/>
                </StackPanel>

                <!-- Close Button -->
                <Button Grid.Column="1" Style="{StaticResource IconButton}" Click="CloseButton_Click">
                    <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                          Width="12" Height="12" Stretch="Uniform"/>
                </Button>
            </Grid>

            <!-- ═══════════════════════════════════════════════════════════════
                 CONTENT
            ═══════════════════════════════════════════════════════════════ -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="24,24,24,0">
                <StackPanel>

                    <!-- Info Message -->
                    <Border Background="#21262D" CornerRadius="12" Padding="16" Margin="0,0,0,24">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="M13,9H11V7H13M13,17H11V11H13M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2Z"
                                  Fill="#58A6FF" Stretch="Uniform" Width="20" Height="20" Margin="0,0,12,0"/>
                            <TextBlock Text="Use an alternative method to verify your identity" 
                                       Foreground="#8B949E" FontSize="13" VerticalAlignment="Center"
                                       TextWrapping="Wrap" FontFamily="Segoe UI Variable"/>
                        </StackPanel>
                    </Border>

                    <!-- PIN Entry Section -->
                    <TextBlock Text="ENTER PIN" FontSize="11" FontWeight="SemiBold" 
                               Foreground="#8B949E" Margin="0,0,0,12"
                               FontFamily="Segoe UI Variable"/>
                    
                    <Border Background="#21262D" CornerRadius="12" BorderBrush="#30363D" 
                            BorderThickness="1" Margin="0,0,0,8">
                        <PasswordBox Name="PinBox" Height="50" FontSize="24" 
                                     MaxLength="6" HorizontalContentAlignment="Center"
                                     Background="Transparent" Foreground="#F0F6FC"
                                     BorderThickness="0" Padding="16,0"
                                     FontFamily="Cascadia Code, Consolas"/>
                    </Border>
                    
                    <TextBlock Text="Enter your 6-digit security PIN" FontSize="12" 
                               Foreground="#484F58" Margin="0,0,0,24"
                               FontFamily="Segoe UI Variable"/>

                    <!-- Divider -->
                    <Grid Margin="0,0,0,24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Height="1" Background="#30363D" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="OR" Foreground="#484F58" FontSize="12" Margin="16,0"
                                   FontFamily="Segoe UI Variable"/>
                        <Border Grid.Column="2" Height="1" Background="#30363D" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Windows Hello Button -->
                    <Button Name="WindowsHelloButton" Style="{StaticResource ModernButton}"
                            Click="WindowsHelloButton_Click" HorizontalAlignment="Stretch" Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="M12,9A3,3 0 0,0 9,12A3,3 0 0,0 12,15A3,3 0 0,0 15,12A3,3 0 0,0 12,9M12,17A5,5 0 0,1 7,12A5,5 0 0,1 12,7A5,5 0 0,1 17,12A5,5 0 0,1 12,17M12,4.5C7,4.5 2.73,7.61 1,12C2.73,16.39 7,19.5 12,19.5C17,19.5 21.27,16.39 23,12C21.27,7.61 17,4.5 12,4.5Z"
                                  Fill="#58A6FF" Stretch="Uniform" Width="20" Height="20" Margin="0,0,12,0"/>
                            <TextBlock Text="Use Windows Hello" FontSize="14"/>
                        </StackPanel>
                    </Button>

                    <!-- Smart Card Button -->
                    <Button Name="SmartCardButton" Style="{StaticResource ModernButton}"
                            Click="SmartCardButton_Click" HorizontalAlignment="Stretch">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="M20,8H4V6H20M20,18H4V12H20M20,4H4A2,2 0 0,0 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V6A2,2 0 0,0 20,4Z"
                                  Fill="#A371F7" Stretch="Uniform" Width="20" Height="20" Margin="0,0,12,0"/>
                            <TextBlock Text="Use Smart Card" FontSize="14"/>
                        </StackPanel>
                    </Button>

                </StackPanel>
            </ScrollViewer>

            <!-- ═══════════════════════════════════════════════════════════════
                 FOOTER
            ═══════════════════════════════════════════════════════════════ -->
            <Border Grid.Row="2" Margin="24,16,24,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="Cancel" Style="{StaticResource ModernButton}"
                            Click="CancelButton_Click"/>
                    <Button Grid.Column="2" Content="Verify PIN" Style="{StaticResource PrimaryButton}"
                            Click="AuthenticateButton_Click"/>
                </Grid>
            </Border>

        </Grid>
    </Border>
</Window>
